[project]
name = "delivery-layer"
version = "0.1.0"
description = "Delivery-aware position layer for voice agents: track what the listener actually heard."
requires-python = ">=3.11"
dependencies = ["websockets>=12.0", "requests>=2.31"]

[tool.pytest.ini_options]
testpaths = ["tests"]
