websockets>=12.0
requests>=2.31
# tests
pytest>=8.0
# optional: number round-trip ASR
# faster-whisper>=1.0

# web demo server (examples/policy-reader/server.py): HTTP + /ws/audio on one port
aiohttp>=3.9

# ingestion (build-time only) — scripts/ingest.py. Not imported by the agent path.
pypdf>=4.0
python-docx>=1.1
beautifulsoup4>=4.12

# voice input (examples/policy-reader/voice/): LiveKit room -> VAD -> a
# direct OpenAI /v1/audio/transcriptions REST call (one shot per VAD-isolated
# utterance -- see voice/bridge.py's module docstring for why not the
# streaming/realtime STT plugin) -> the existing /ws/audio interrupt/ask
# protocol. Needs LIVEKIT_URL, LIVEKIT_API_KEY, LIVEKIT_API_SECRET,
# OPENAI_API_KEY.
livekit-agents[silero]~=1.6
openai>=1.50
python-dotenv>=1.1
